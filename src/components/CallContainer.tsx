import Stack from '@mui/material/Stack';
import { styled } from '@mui/material';

export const CallContainer = styled(Stack)({
  backgroundColor: '#181c1f', // '#1a1a1a'
  position: 'absolute',
  left: 0,
  top: 0,
  right: 0,
  bottom: 0,
  zIndex: 1101,
});